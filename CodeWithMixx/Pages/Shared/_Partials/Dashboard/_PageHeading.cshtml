@using System.Globalization
@model (string Title, string Subtitle)

@{
    var currentDate = DateTime.Now.ToString("dd. MMMM yyyy.", new CultureInfo("sr-Latn-RS"));
    var currentYear = DateTime.Now.Year;
}


<div class="flex justify-between gap-3 sm:flex-row sm:items-center bg-emerald-900/60 px-4 py-2 rounded-lg shadow-lg ">
    <div class="">
        <h1 class="text-3xl font-bold text-white tracking-tight">@Model.Subtitle</h1>
        <p class="mt-0.5 text-sm text-emerald-300/70">@Model.Title</p>
    </div>
    <div class="flex items-center gap-2 self-center rounded-lg bg-white/5 px-4 py-2 text-sm text-emerald-100/80 backdrop-blur-sm sm:self-auto">
        <i class="fa-regular fa-calendar text-emerald-400"></i>
        <span>@currentDate</span>
    </div>
</div>